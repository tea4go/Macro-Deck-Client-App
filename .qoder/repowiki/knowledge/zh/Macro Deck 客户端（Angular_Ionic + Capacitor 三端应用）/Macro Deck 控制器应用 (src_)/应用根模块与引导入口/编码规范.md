- 根组件采用构造函数注入所有核心单例服务，并将所有异步初始化逻辑集中在 ngOnInit 中按固定顺序 await 执行，保证依赖就绪后再进行平台特性配置。
- 平台特定行为通过 DiagnosticService 的 isAndroid()/isiOSorAndroid() 等运行时方法分支处理，避免引入 platform-specific 目录或编译时条件编译。
- 跨组件通信优先使用 AppComponent 上的静态 EventEmitter（如 quickSetupLinkScanned），由根组件统一接收外部事件并向上游广播。