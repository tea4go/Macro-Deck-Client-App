- UI 可见性控制集中在 MainActivity，通过统一的 uiVisibilityFlags 常量组合 IMMERSIVE_STICKY 等标志位，并在生命周期回调中强制恢复全屏。
- Gradle 版本管理采用集中式 variables.gradle，所有 SDK 与第三方库版本号统一定义在 ext {} 块中供根脚本与子模块共享。
- Fastlane 车道通过 ENV[...] 注入敏感信息（keystore、Play Store 凭证），不在代码中硬编码任何密钥或路径。