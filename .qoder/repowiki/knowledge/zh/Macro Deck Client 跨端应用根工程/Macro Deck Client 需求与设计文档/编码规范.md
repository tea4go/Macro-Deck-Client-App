- 所有对外通信采用 Protocol v2 的 JSON 结构，统一以 `Method` 字段区分消息类型，客户端→服务器与服务器→客户端分别维护独立的消息清单。
- 数据模型以 TypeScript interface 形式在文档中显式声明（如 Connection、Widget、ButtonWidget），并在多处重复引用以保持跨章节一致性。
- 服务类采用单例 class 设计，通过 async 方法暴露能力并以 EventEmitter 发布状态变更事件，职责边界清晰且无继承关系。