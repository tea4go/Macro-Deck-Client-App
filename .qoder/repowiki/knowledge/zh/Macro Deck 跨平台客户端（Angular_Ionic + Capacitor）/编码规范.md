- 每个页面采用 Page + Module 成对组织，并附带同名 `.spec.ts` 单元测试文件
- 服务类按功能域划分到 `src/app/services/<domain>/` 目录，并以 `<domain>.service.ts` 命名
- 微件内容组件遵循 `widget-content-components/<type>/<type>-widget.component.*` 的统一命名约定