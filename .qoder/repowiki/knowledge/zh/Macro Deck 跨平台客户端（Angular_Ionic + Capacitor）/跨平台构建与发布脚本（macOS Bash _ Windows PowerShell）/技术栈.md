Bash 3.2+（macOS 默认）与 PowerShell 5.1+（Windows 默认）双栈实现；深度集成 Ionic CLI、Capacitor 7、fastlane（优先 `bundle exec fastlane`）、npm (`--legacy-peer-deps`) 以及 Android SDK cmdline-tools；Windows 侧额外封装了并行测速下载、NTFS junction 路径解析、Gradle wrapper 镜像替换等工具链适配。