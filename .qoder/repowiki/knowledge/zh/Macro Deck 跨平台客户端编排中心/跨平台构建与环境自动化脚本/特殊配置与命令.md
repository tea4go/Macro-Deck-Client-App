- **环境引导**：Unix 执行 `./scripts/macos/install_1_base_tools_bymac.sh` 等序列；Windows 执行 `.\scripts\windows\install_1_base_tools_bywin.ps1`。
- **Android 签名配置**：需复制 `scripts/local/android-signing.env.example` 为 `android-signing.env` 并填写 `KEYSTORE_FILE_PASSWORD`。
- **iOS 构建要求**：仅限 macOS，需预先配置 fastlane match 及 App Store Connect API 密钥环境变量。