- 采用 Angular 标准引导架构：`AppModule` 为根模块，通过 `bootstrap: [AppComponent]` 启动应用。
- `AppComponent` 作为根组件，利用 `<ion-nav>` 动态加载 `rootComponent`（根据环境区分 Web 或原生首页）。
- 在 `ngOnInit` 中串行初始化核心跨切面服务（存储、国际化、主题、屏幕方向、常亮），并注册 Capacitor 深度链接监听器。
- 集成 Service Worker 模块，配置为非开发模式下延迟注册，以支持 PWA 功能。