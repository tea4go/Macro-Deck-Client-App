- **环境要求**：执行 `fastlane build` 前必须设置 `BUILD_NUMBER`、`VERSION_NUMBER`、`KEY_ID`、`ISSUER_ID` 及 `KEY_CONTENT` 等环境变量。
- **证书同步**：首次运行需确保拥有访问 `git@github.com:Manuel-Mayer-Software-Development/fastlane.git` 的权限以获取匹配的代码签名证书。