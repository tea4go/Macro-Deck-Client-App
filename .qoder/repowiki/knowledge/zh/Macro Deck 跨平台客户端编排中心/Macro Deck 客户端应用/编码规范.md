- 服务类统一使用 `@Injectable({ providedIn: 'root' })` 声明为全局单例，并通过构造函数注入依赖。
- 页面组件实现 Ionic 生命周期接口（如 `ViewDidEnter`, `ViewDidLeave`）以管理订阅初始化与资源释放。
- 数据模型定义在 `src/app/datatypes` 目录下，使用 TypeScript `interface` 严格描述协议结构与 UI 状态。
- 异步操作（Storage 读写、Modal 交互）统一采用 `async/await` 语法以保持逻辑线性。