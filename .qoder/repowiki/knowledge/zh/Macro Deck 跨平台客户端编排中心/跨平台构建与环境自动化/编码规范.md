- 所有 Shell 脚本强制启用严格模式 (set -euo pipefail) 以确保错误即时中断。
- 在调用任何外部构建工具前，必须通过 require_command 或 Require-Command 显式校验其可用性。
- 构建流程遵循“Web 资源构建 -> Capacitor 同步 -> 原生平台打包”的标准流水线顺序。
- Windows 脚本统一通过 dot-sourcing (.) 引入 _common.ps1 以复用日志输出与环境变量管理逻辑。