构建需注入环境变量 BUILD_NUMBER、VERSION_NUMBER、KEYSTORE_FILE_PATH、KEYSTORE_FILE_PASSWORD、KEYSTORE_FILE_ALIAS；执行 bundle exec fastlane build 进行签名打包并生成重命名的 AAB/APK；执行 bundle exec fastlane release 上传 AAB 至 Google Play Store（草稿状态），需提供 PLAYSTORE_CREDENTIALS 服务账号 JSON。